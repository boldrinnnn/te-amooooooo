<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Para a Pessoa Mais Especial</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 0;
            background: linear-gradient(120deg, #ff9aa2, #fcb1c8, #fde2e4);
            color: #fff;
            text-align: center;
            overflow: hidden;
            position: relative;
        }

        header {
            background-color: rgba(255, 105, 180, 0.8);
            padding: 20px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.2);
            border: 5px solid #ff6f91;
            border-radius: 15px;
        }

        h1 {
            font-size: 3em;
            margin: 20px 0;
            text-shadow: 2px 2px 10px rgba(0, 0, 0, 0.2);
        }

        .heart {
            font-size: 5em;
            margin: 20px;
            color: #ff6f91;
            animation: beat 1.5s infinite;
        }

        @keyframes beat {
            0%, 100% {
                transform: scale(1);
            }
            50% {
                transform: scale(1.2);
            }
        }

        .message {
            margin: 30px auto;
            max-width: 800px;
            padding: 20px;
            background: rgba(255, 182, 193, 0.3);
            border-radius: 15px;
            border: 5px double #ff6f91;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
        }

        .image-container {
            margin: 20px auto;
            width: 50%;
            max-width: 400px;
        }

        .image-container img {
            max-width: 100%;
            height: auto;
            border-radius: 15px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
        }

        footer {
            margin-top: 30px;
            padding: 10px;
            font-size: 0.9em;
        }

        /* Botões estilizados */
        .button-container {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin-top: 30px;
        }

        .magic-button, .link-button {
            padding: 15px 25px;
            color: white;
            font-size: 1.2em;
            font-weight: bold;
            border-radius: 50px;
            cursor: pointer;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            transition: background-color 0.3s, transform 0.3s;
            text-decoration: none;
        }

        .magic-button {
            background-color: #ff4f6f;
        }

        .magic-button:hover {
            background-color: #ff2f4f;
            transform: scale(1.1);
        }

        .link-button {
            background-color: #6fafff;
            text-decoration: none;
        }

        .link-button:hover {
            background-color: #4f8fff;
            transform: scale(1.1);
        }

        /* Corações flutuantes */
        .floating-hearts {
            position: absolute;
            width: 100%;
            height: 100%;
            top: 0;
            left: 0;
            pointer-events: none;
        }

        .heart-float {
            position: absolute;
            font-size: 2em;
            color: #ff6f91;
            animation: floatUp 6s infinite ease-in-out;
        }

        @keyframes floatUp {
            0% {
                transform: translateY(100vh) scale(1);
                opacity: 0.8;
            }
            50% {
                transform: translateY(50vh) scale(1.2);
                opacity: 1;
            }
            100% {
                transform: translateY(-10vh) scale(1);
                opacity: 0.6;
            }
        }
    </style>
</head>
<body>
    <header>
        <h1>Para Você, Meu Amor</h1>
    </header>

    <!-- Corações flutuantes -->
    <div class="floating-hearts" id="floating-hearts">
        <!-- Corações animados serão adicionados pelo JavaScript -->
    </div>

    <!-- Substituindo a imagem -->
    <div class="image-container">
        <img src="https://i.imgur.com/KTNIpBb.jpeg" alt="Imagem especial">
    </div>

    <div class="heart">💖</div>

    <!-- Texto no meio -->
    <div class="message">
        <p>Você é a luz que ilumina meus dias e a inspiração por trás de cada sorriso meu.</p>
    </div>

    <!-- Botões lado a lado -->
    <div class="button-container">
        <button class="magic-button" onclick="createHearts()">Clique aqui para mais amor 💕</button>
        <a href="https://www.youtube.com/watch?v=dQw4w9WgXcQ" target="_blank" class="link-button">Ouvir Música 🎵</a>
    </div>

    <footer>
        <p>Com todo o meu carinho, [Seu Nome]</p>
    </footer>

    <script>
        // Criar corações flutuantes de baixo para cima
        function createHearts() {
            const floatingHearts = document.getElementById('floating-hearts');
            for (let i = 0; i < 10; i++) {
                const heart = document.createElement('div');
                heart.className = 'heart-float';
                heart.style.left = `${Math.random() * 100}%`;
                heart.style.animationDelay = `${Math.random() * 3}s`;
                floatingHearts.appendChild(heart);
                heart.textContent = '💖';

                // Remover o coração após a animação
                setTimeout(() => heart.remove(), 6000);
            }
        }

        // Inicializa os corações animados ao carregar a página
        setInterval(createHearts, 2000);
    </script>
</body>
</html>
